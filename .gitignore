.env
__pycache__/
*.pyc
.cache/
cortes/
metrics/*.json
videos/
*.mp4
*.mkv
# Artefatos de diagnóstico (--debug-captions): nunca versionar.
debug/
# Artefatos locais pesados (não versionar; ver README § Intel Arc):
# - thirdparty/: whisper.cpp (fonte+build, ~370 MB) e SPIRV-Headers/prefix
# - models/: ggml-medium.bin (~1,5 GB, HuggingFace ggerganov/whisper.cpp)
thirdparty/
models/
.ai-jail
# Documentação (docs/) é versionada: nunca ignorar *.md / *.pdf aqui.
# PDFs em docs/ são espelhos dos .md (mesmo basename) ou referências técnicas.
# Sessões de revisão humana (work/<video>/): intermediários locais.
work/